[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = ["cd backend/chatbot && pip install -r requirements.txt"]

[start]
cmd = "cd backend/chatbot && gunicorn api_chat:app"
