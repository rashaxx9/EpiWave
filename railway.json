{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "gunicorn api_chat:app",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "requestTimeout": 300
  }
}
