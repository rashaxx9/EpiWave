{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd chatbot && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd chatbot && gunicorn api_chat:app",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
